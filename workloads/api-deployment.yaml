---
apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp:
  labels:
    app: api
  name: api
  namespace: kaori
  annotations:
    fluxcd.io/automated: "true"
    fluxcd.io/tag.api: glob:master-*
spec:
  replicas: 3
  selector:
    matchLabels:
      app: api
  template:
    metadata:
      creationTimestamp:
      labels:
        app: api
    spec:
      containers:
      - command:
        - gunicorn
        args:
        - --config
        - python:config.gunicorn_api
        - kaori.api:app
        envFrom:
        - configMapRef:
            name: web-config
        image: us.gcr.io/kizuna-188702/kaori/kaori:master-d07e8b7833e598913611e371c6dcce7e32268078
        imagePullPolicy: Always
        name: api
        ports:
        - containerPort: 8001
      restartPolicy: Always
