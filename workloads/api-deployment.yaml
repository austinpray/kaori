---
apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp:
  labels:
    app: api
  name: api
  namespace: kaori
  annotations:
    fluxcd.io/automated: "true"
    fluxcd.io/tag.api: glob:master-*
spec:
  replicas: 3
  selector:
    matchLabels:
      app: api
  template:
    metadata:
      creationTimestamp:
      labels:
        app: api
    spec:
      containers:
      - command:
        - gunicorn
        args:
        - --config
        - python:config.gunicorn_api
        - kaori.api:app
        envFrom:
        - configMapRef:
            name: web-config
        image: us.gcr.io/kizuna-188702/kaori/kaori:master-2fa2597ceb4f3f63113f822411ca019e5e3e77ef
        imagePullPolicy: Always
        name: api
        ports:
        - containerPort: 8001
      restartPolicy: Always
